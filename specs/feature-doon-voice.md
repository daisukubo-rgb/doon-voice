# DOON Voice 機能仕様

## 現行仕様（0.5.30、2026-09-18）

本節を現行仕様とする。後半の日付付き記述は過去の実装・検証履歴であり、今回の実機試験結果ではない。0.5.19はWindows x64のテスト配布版。Windows上でMSIを生成し、実インストール・起動・アンインストールまで検査したうえで、手順と権利表示を同梱したZIPを作る。GitHubの公開Releaseは更新しない。

クラウドAIの事前接続は、再利用できるChatGPTだけを最大5秒で試す。発話ごとにCLIを終了するClaude／Geminiは事前接続しない。外部サービス自体の応答時間は残るが、不要な事前接続が通常の回答を最大90秒待たせない。

## 目的と操作

Mac／Windowsで音声を端末内のwhisper.cppとlarge-v3-turboモデルにより文字にする。初期キーはCtrl+Alt+Space（変更可能、既存の保存値は維持）。同じキーで録音を開始・停止し、原文または整形結果をクリップボードと現在の入力先へ渡す。OSへの貼り付け操作の送信は確認できるが、入力先アプリの受領までは確認しない。処理中は入力先を切り替えない。

初回設定にはマイクの許可ボタンを置く。押すとOSのマイク許可を要求し、許可直後に試験用のマイク接続を閉じる。音声認識モデル、Ollamaインストーラー、ローカルGemmaモデルの取得中は、配布元への接続・ダウンロード・展開などの工程を表示する。総容量を取得できる場合は取得済み容量、全容量、進捗、実測速度から算出した残り時間の目安を表示する。総容量が分からない工程では、推測の％や時間を表示しない。

Whisper前の検査は、完全無音・量子化程度の信号・短い単発音を除外する。実際の録音レートで20ms区間と60ms分の継続信号を数え、固定の大きな音量や冒頭無音を要求しない。振幅から発話の意味や背景音との区別を保証するものではない。

画面を閉じても常駐側が録音・処理を管理する。状態は待機・マイク準備・録音・処理に分かれる。マイク準備と文字起こし／AI処理は画面から中止できる。処理中の再録音は開始しない。

macOSのメニューバー／Windowsの通知領域に常駐アイコンを置き、「DOON Voiceを開く」で元の画面を再表示する。「DOON Voiceを終了」および通常の終了要求では、マイク準備・録音・処理中だけ終了を止めて画面を開く。未コピーの結果だけでは終了を止めない。強制終了やOS終了時の保存は保証しない。

## 文章の仕上げ

- AIなし: 文字起こしを外部AIやOllamaへ渡さない。音声認識モデルは必要。
- このPCのAI: 127.0.0.1のOllamaでGemma 4 E2Bを使用。本文生成なしの事前読込は維持。
- ChatGPT: Codex CLI。常駐プロセス上で発話ごとの一時スレッドを作る。
- Claude: Claude Code。発話ごとにCLIを終了し、次の発話へ会話を使い回さない。
- Gemini: Antigravityのagy CLI。Claudeと同じく発話ごとにCLIを終了する。
- クラウドへの送信対象は本文と登録済み辞書。認証・提供者の保存方針は各CLIの管理範囲。

AIは意味を足さずに、句読点・助詞・語尾を読みやすい書き言葉へ整え、「えー」「えっと」「あの」「あと」など意味を足さないフィラーや言い直しを除く。前後文脈と辞書から明白な音声認識誤りだけを漢字・固有名詞へ直し、確信できない語は原文を残す。自動採用前に数値・符号、否定、視点、2文字以上の漢字の事実アンカー、文字の重なりと文章量を照合する。URL・メールアドレスを含む文は原文を採用する。これは文字起こし自体の正確さの保証ではない。

AIで整えた結果が180文字以上のときは、意味確認の後にアプリが「。」の直後だけへ段落改行を追加する。おおむね120文字以上が続いた次の文末で区切る。さらに「1つ目」「2つ目」のように二つ以上の連番がある話し方は、アプリが改行と `- ` を追加して箇条書きにする。語句・数字・URL・既存の文字は変更せず、AIなしでは段落分け・箇条書き化しない。

ホームの「選択した文章を質問」は、アクセシビリティ許可済みなら利用者が選択した本文を自動取得し、未許可の場合はコピー済みクリップボードの本文を表示し、入力または音声で質問を受け取る。ほかのアプリで文章を選択したまま通常の開始・停止キーで質問を話した場合も、ボタンを押さずに選択文質問として処理する。選択がない場合は従来どおり通常の音声入力にする。ショートカットで得た回答はDOON Voice本体を開かない独立ウィンドウで表示し、コピーして利用する。ChatGPTを選んだ調査・最新情報の質問では必要な場合だけWeb検索を使い、確認した事実と出典URLを示す。質問欄は変換確定のEnterでは送信せず、もう一度Enterで送信、Shift+Enterで改行、送信ボタンでも送信する。回答にはAIなしを使えない。選択文中の命令・URL・コード・役割変更指示は引用データとして扱い、実行しない。

文字起こしの原文は前後の空白を除いて保持する。文頭のフィラーや固有名詞、文字列として含まれる `</think>` は削除しない。思考タグ以前の除去はAIから返された文章だけに適用する。

Codexは完了した自分のturnから最終本文だけを採用する。途中解説・別thread/turn・failed/interruptedは完成文として扱わない。通常の挨拶や反復した業務文は、それだけを理由に文字起こしから削除しない。

## 回収と異常処理

- 原文と整形結果は貼り付けより先にメモリへ保持する。アプリを終了すると失われる。
- 失敗時は原文コピー・結果コピー・再試行・破棄を提供する。別クラウドへの自動転送はしない。
- エラーや未コピーの結果が残っていても、次の録音と選択文質問を開始できる。新録音の開始時に前回の結果を置き換える。各操作は処理世代番号を照合し、遅い前回操作から新しい結果を守る。
- クリップボード保存成功と貼り付け操作成功を別管理する。保存に失敗した場合は保存済みと表示しない。
- マイク切断・上限到達・文字起こしの異常終了では、取得できた部分音声／本文を保持する。部分本文は再試行しても自動貼り付けせず、確認・コピーを求める。
- Whisperには300秒の期限、終了コード確認、キャンセル、出力量制限を設ける。CLI認証確認は5秒、AI通信は既存90秒を基本とし、待ち中も取消を確認する。
- AIのCLIが応答せず終了した場合は標準出力の終了を検出し、通常の応答期限まで待たず失敗を返す。終了直前の最終JSONは順番どおり受け取り、エラー詳細の読取待ちは100msまでに制限する。
- 録音上限は15分と240MiB（WAVヘッダー44bytesを含む）の小さい方。端末の異常待ちは有限にし、音声サンプルの不要な全量複製はしない。
- 録音ファイルは専用領域で一意に作成し、成功・失敗・中止時に削除を試みる。起動時は24時間以上の自分の録音と旧形式の数値名WAVだけを回収する。強制終了や削除失敗時の残存はあり得る。

## 設定と配布

辞書は100件、各1〜80 Unicodeコードポイント。画面とバックエンドを揃え、不正・過大な保存データを黙って切り捨てない。設定は保存成功後に画面へ確定し、保存失敗／初期同期前はOSショートカットからの開始も止める。キー捕捉は画面移動・ぼかし・Escで終了し、古い解除応答から新しいキーを守る。ログイン完了が遅れた場合も、アプリ内で接続を要求したサービスだけを再確認する。

マイク準備・録音・処理中はAIと辞書の変更を常駐側で拒否する。同一設定の同期は許容し、確定済み設定を無効化しない。AI選択は両画面で無効にし、保存待ちキューの実行時にも状態を再確認する。

ソース起動はOS別ダブルクリックから環境確認・npm導入・起動へ進む。不足するNode/Rust/OSビルド環境をすべて自動導入するものではない。WindowsのnpmはNodeから実npmエントリーを起動する。

梱包は現バージョンと対象機種に一致する単一インストーラーに限定する。LICENSE/NOTICE・入手元・ハッシュを同梱し、実インストーラー展開後にも照合する。公開はタグのコミットを固定し、全対象の品質・ビルド検査後のみ実施する。

macOS版whisper-cliはWhisper／GGML／Metalシェーダーを本体へ組み込み、OS標準ライブラリのみを動的参照する。Hardened Runtimeとライブラリ検証を維持し、DMGに入った署名済みエンジンの実起動も配布検査に含める。

macOSの親アプリは`com.apple.security.device.audio-input`を署名時に宣言する。最終DMGから署名宣言を抽出して真偽値を検査する。ad-hoc署名で更新した場合、既存のOS許可との識別情報が変わり、マイクやアクセシビリティの再許可が必要になることがある。

Windowsの音声エンジンは公式whisper.cpp v1.9.2の固定ソースをMSVC /MTでビルドし、Whisper／GGML／VCランタイムを本体へ組み込む。OpenMPを無効にした互換版とAVX2版を同梱する。CPUのSSE4.2／AVX／AVX2／FMA／F16Cがすべて利用可能ならAVX2版、それ以外は互換版を使う。高速版を選べるCPUで必要ファイルが欠けていた場合は明示的にエラーにする。従来のWhisper／SDL2／MSVC DLLは同梱しない。単独EXEとMSI内の選択EXEの両方で、本番と同じlarge-v3-turbo-q5_0モデルによる固定音声の認識を試験する。互換版は検証環境で5分の上限を超えており、古いCPUでの認識速度は保証しない。

WindowsのAI連携はPATH上の実EXEまたは標準npm起動ファイルから実体を解決し、Nodeスクリプト／native EXEを直接起動する。認証確認・JSON通信はコマンドシェルへ本文を渡さない。追加のシェル演算を含む独自起動ファイルは明示的に拒否する。通常の子プロセスはコンソールを表示せず、利用者が明示したログイン操作だけ新しいコンソールを開く。

## 検証範囲

Windowsテスト配布のビルド・認識・MSI・画面検査の結果と未検証条件は [0.5.19の記録](../docs/QA-0.5.19.md) を参照する。Windows 10/11 x64向けで、ARMネイティブ版と32ビット版は対象外。Windows CIでの確認は、利用者のPCでの実マイク録音、入力先アプリへの貼り付け、実アカウントでの外部AI通信の成功を保証するものではない。

### 0.5.18の検証履歴

音量・無音区間・サンプルレートの回帰、模擬子プロセス、録音・ファイル管理の単体試験、型・Rust品質検査、署名後エンジンと日本語合成音声による実認識を行う。詳細な結果・未検証条件は [音声取得エラーの修正記録](../docs/QA-0.5.18.md) に記録する。画面と前版の検証は [0.5.17の記録](../docs/QA-0.5.17.md) を参照。

0.5.18の実マイク試験ではmacOSの署名宣言不足による拒否を確認して修正し、更新後のOS再許可待ちまで確認した。その時点でWindows新規端末への導入、外部AI・実貼り付けは検証していない。0.5.19でWindowsの旧DLL依存を解消した。sigchldの不足表示など権利資料の残余事項は [OSS表示](../docs/OSS-NOTICES.md) に記録する。

## 2026-08-29 改訂

実機のCodex連携で、短いNDJSON応答を読み取らずタイムアウトする不具合を確認した。パイプから利用可能なデータを逐次読み、JSON行をバッファから独立コピーしてから解析する方式へ修正した。短い応答の回帰テストとOllama互換APIテストを追加した。

## 2026-08-30 旧Swift試作のUI提供状況

DOON VoiceはDockに常時表示する一般的なウィンドウアプリではなく、macOSのメニューバー常駐アプリとしてUIを提供する。このため、Dockに画面が見当たらなくてもUI未実装ではない。

| 入口 | できること |
|---|---|
| メニューバーの波形アイコン | 録音開始・停止、現在の認識文と処理状態の確認、整形エンジンの切替 |
| 「設定」 | ローカルLLM/Codexの設定、置換辞書、アクセシビリティ許可 |
| `Control + Option + Space` | どの入力欄にいても録音の開始・停止 |

この構成は旧Swift試作の記録である。2026-08-31以降はMac／Windowsへ配布するTauriデスクトップ版を製品実装とする。

## 2026-08-31 デスクトップ配布版の販売可能化

### 目的

Mac／Windows向けのDOON Voice Desktopで、音声を端末内で文字起こしし、選択したAIで整えてクリップボードへ渡す。

### フロー

1. 利用者が音声認識モデルを端末へ取得する。
2. 開始・停止キーまたは「話す」で録音する。
3. 端末内Whisperが日本語音声を文字にする。
4. ChatGPT、Claude、このPCのAIのいずれかが文章を整える。
5. 結果を現在の入力欄へ直接貼り付け、同時にクリップボードへ保存する。音声入力の履歴は保存しない。

### 安全条件

- 録音音声は既定で保存せず、一時ファイルを処理後に削除する。
- ChatGPT／Claudeを選んだ場合だけ、文字起こし結果を各公式CLIへ渡す。
- APIキー・パスワード・認証トークンはアプリで取得・保存しない。
- 音声入力の履歴は保存しない。利用者が登録した辞書だけを端末内へ保存し、画面から削除できる。
- macOSの直接貼り付けにはアクセシビリティを利用し、未許可時は設定画面への導線とクリップボードの代替手段を表示する。

### 改訂履歴

- 2026-09-18: v0.5.30で、音声認識モデル・Ollama・ローカルGemmaモデルの取得工程と、取得済み容量・全容量・進捗・残り時間の目安を設定画面に表示した。初回マイク許可ボタンを追加し、他PCでも録音開始前にOSの許可画面を開けるようにした。
- 2026-09-18: v0.5.29で、話し言葉のフィラー除去、文脈に沿う漢字・固有名詞補正、自然な書き言葉への整形を追加した。数値・否定・視点・漢字の事実アンカーを照合し、危険な書き換えは原文へ戻す。エラーまたは未コピーの結果を破棄せず、新しい録音や選択文質問を開始できるようにした。
- 2026-09-18: v0.5.28で、質問用キーによる選択文の取得を改善した。DOON Voice本体で選択した文章も取得対象にし、コピー後のクリップボード反映を最大200ms待ってから失敗と判定する。
- 2026-09-18: v0.5.26で、選択文質問の回答をDOON Voice本体と別の専用ウィンドウで表示するよう変更した。ショートカットから回答・エラーが返ってもメイン画面を前面へ出さず、専用ウィンドウで再質問、コピー、カーソル位置への入力まで完結する。ChatGPT選択時の調査・最新情報の質問は、必要な場合にWeb検索を使い、確認した事実と出典URLを回答へ含める。
- 2026-09-18: v0.5.25で、通常の開始・停止キーと、選択文へ音声で質問するキーを分離した。既定の質問用キーはCtrl+Alt+Q（MacではControl+Option+Q）。接続と設定から両方を別々に変更・標準復帰でき、同じ組み合わせは登録できない。質問用キーで選択文を取得できない場合は、通常の音声入力へ切り替えずエラーを表示する。
- 2026-09-18: v0.5.25で、クラウドAIの事前接続をChatGPTだけ・5秒上限に変更し、Claude／Geminiの不要な事前接続による待機をなくした。選択文の取得は直前のクリップボードとの差ではなく一時的な検査文字列との差で判定するため、同じ文章を選び直しても質問できる。回答生成に失敗しても質問ポップアップを表示し、理由を確認して再試行できる。
- 2026-09-18: v0.5.25で、外部アプリで文章を選択した状態の開始・停止キーによる音声を、ボタンなしの選択文質問として処理するようにした。回答後は自動貼り付けせず、回答ポップアップでコピーまたは入力を選べる。選択がない場合は従来どおり音声入力として扱う。
- 2026-09-04: GitHubから取得したフォルダ内のOS別ランチャーをダブルクリックすると、初回セットアップを行ってDOON Voiceを起動する導線を追加した。2回目以降はpackage-lock.jsonが変わっていなければセットアップを省略する。
- 2026-09-03: v0.5.15でローカルAIの事前読込と、ChatGPT・Claude・Gemini公式CLIの常駐接続再利用を追加した。実機でGemma 4 E2Bの自動読込とChatGPT常駐起動を確認した。
- 2026-09-18: v0.5.22で、選択文質問のChatGPT用指示を回答用途へ分離し、アクセシビリティ許可済みのMac／Windowsでは選択した文章を自動コピーして取得するようにした。選択文は6,000文字、質問は2,000文字までに制限し、外部AIへの過大な送信による待ち時間を防ぐ。Claudeの認証期限切れは再ログインを明示する。
- 2026-09-03: ホーム見出しを「AIで言語化をイージーに」へ変更し、RocknRoll Oneを製品内へ同梱した。PC・スマホとも見出しを1行で表示する。

- 2026-08-31: デスクトップ配布版の実働フロー・安全条件を追加（2026-08-31-doon-voice-sale-ready）
- 2026-08-31: 開始・停止キーで入力欄を離れずに使える、カーソル位置への直接入力を追加。Windowsは貼り付けキー、macOSはアクセシビリティ許可後の貼り付けキーを使う。
- 2026-08-31: DOON Voice単体で完結する構成へ変更。ローカル整形は非Qwen系のGemma 4 E2BをOllama公式レジストリから取得し、音声認識はDOON Voice自身の保存先に `large-v3-turbo` を取得する。ChatGPTはGPT-5.6 Luna、ClaudeはHaikuに固定する。実機でGemma 4 E2Bの文章整形を3件検証し、モデル保持後は0.58〜0.96秒で応答した。
- 2026-08-31: v0.5.2でDOON公式ロゴ中心のUIへ変更。アクセシビリティの許可状態を画面に表示し、未許可時はmacOSの設定を開く。AIが主語・視点・数字を変えた場合に元の文字起こしへ戻す意味保持ガードを追加した。
- 2026-08-31: DOON公式サイトの人物グリフをナビゲーション、AI選択、設定項目へ適用した。状態語を `接続可能`、`選択中`、`モデル取得済み`、`稼働中` に分け、ChatGPT／Claudeの操作名を `接続する` に統一した。
- 2026-08-31: 見出しを「AIで言語化を楽に」へ変更し、余白と行高を縮小した。公式CLIの実認証状態を確認し、`接続中` から `接続済み` へ遷移するよう変更。音声入力履歴の画面・端末保存を廃止した。
- 2026-08-31: ショートカット操作中の録音・処理・完了・失敗を、他アプリの手前に表示する画面下中央の状態オーバーレイを追加した。
